import 'package:flutter/material.dart';
import 'package:get/get.dart';

import '../../../common/widgets/custom_sized_boxes.dart';
import '../../../common/widgets/custom_text_widgets.dart';

class AboutUsController extends GetxController {
  List<Widget> get firstContent => [
        const TextMul18W700("""👋 I'm Atul maurya, a Flutter developer."""),
        const SBH20(),
        const TextMul18W700("""📱 Passionate about creating mobile apps."""),
        const SBH20(),
        const TextMul18W700("""🎨 Love designing beautiful user interfaces."""),
        const SBH20(),
        const TextMul18W700("""💻 Experienced with Flutter & Dart."""),
      ];
  List<Widget> get secondContent => [
        const TextMul18W700("""👋 I'm Atul maurya, a Flutter developer."""),
        const SBH20(),
        const TextMul18W700("""📱 Passionate about creating mobile apps."""),
        const SBH20(),
        const TextMul18W700("""🎨 Love designing beautiful user interfaces."""),
        const SBH20(),
        const TextMul18W700("""💻 Experienced with Flutter & Dart."""),
      ];
  List<Widget> get thirdContent => [
        const TextMul18W700("""👋 I'm Atul maurya, a Flutter developer."""),
        const SBH20(),
        const TextMul18W700("""📱 Passionate about creating mobile apps."""),
        const SBH20(),
        const TextMul18W700("""🎨 Love designing beautiful user interfaces."""),
        const SBH20(),
        const TextMul18W700("""💻 Experienced with Flutter & Dart."""),
      ];
}
